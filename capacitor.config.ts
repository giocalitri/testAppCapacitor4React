import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'testAppCapacitor4',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
